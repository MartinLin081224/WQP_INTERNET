<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 禁止時 -->
    <item android:state_enabled="false" android:color="@color/colorLightBlue"/>
    <item android:state_pressed="true" android:color="@color/windowBackgroundWhite"/>

    <!-- 預設時 -->
    <item android:state_enabled="true" android:color="@color/windowBackgroundWhite"/>
    <item android:state_pressed="false" android:color="@color/colorLightBlue"/>


</selector>